\documentclass[border=3.141592]{standalone}
\usepackage{listings}

\begin{document}
\begin{lstlisting}[language=c]
void read(int x) {
  if(x<0) {         
    if(x<-3) {
      foo(x);
    } else if(x>-2) {
      bar(x);
    } else {
      ex();
    }
  }
}
\end{lstlisting}
\end{document}
